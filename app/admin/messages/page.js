import { redirect } from 'next/navigation';import BrandHeader from '@/components/BrandHeader';import Flowers from '@/components/Flowers';import { requireAdmin } from '@/lib/supabaseServer';import { getSupabaseAdmin } from '@/lib/supabaseAdmin';
export const dynamic='force-dynamic';
export default async function Messages(){const {allowed,user}=await requireAdmin();if(!user)redirect('/login');if(!allowed)redirect('/login?error=not_allowed');const supabase=getSupabaseAdmin();const {data}=await supabase.from('message_history').select('*, orders(customer_name)').order('created_at',{ascending:false}).limit(100);const messages=Array.isArray(data)?data:[];return <main className="page"><Flowers/><div className="shell"><BrandHeader/><section className="panel"><p className="eyebrow">Manual Messenger log</p><h1 className="h1">Messages</h1><p className="copy">Every copied order message is saved here so you have a record.</p><br/>{messages.length===0?<p className="copy">No messages yet.</p>:messages.map(m=><div className="card" key={m.id} style={{marginBottom:12}}><b>{m.orders?.customer_name||'Customer'}</b><p className="copy">{m.channel} · {m.status}</p><div className="messageBox">{m.message_body}</div></div>)}</section></div></main>}
