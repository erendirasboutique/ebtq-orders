'use client';
import { getSupabaseBrowser } from '@/lib/supabaseBrowser';import Flowers from '@/components/Flowers';
export const dynamic='force-dynamic';
export default function Login(){async function login(){const supabase=getSupabaseBrowser();if(!supabase){alert('Missing Supabase environment variables in Vercel.');return}await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${window.location.origin}/auth/callback`}})}return <main className="page flowerfield"><Flowers/><div className="shell"><section className="customerHero panel"><img className="logo" src="/eb-logo.png" alt="Erendira's Boutique"/><p className="eyebrow">Admin only</p><h1 className="h1">Welcome Back</h1><p className="copy">Sign in with your approved Google account to manage Order Concierge.</p><br/><button className="btn primary" onClick={login}>Continue with Google</button></section></div></main>}
