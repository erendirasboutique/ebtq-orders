'use client';
import { useState } from 'react';
export default function CopyMessageBox({ message, link }){const [copied,setCopied]=useState('');async function copy(text,label){await navigator.clipboard.writeText(text);setCopied(label);setTimeout(()=>setCopied(''),1800)}return <div className="card"><p className="eyebrow">Manual Messenger</p><h2 className="h2">Message Ready</h2><p className="copy">Copy this and paste it into the customer’s Facebook Messenger conversation.</p><div className="messageBox">{message}</div><div className="topActions"><button className="btn primary" onClick={()=>copy(message,'Message copied')}>Copy Message</button><button className="btn purple" onClick={()=>copy(link,'Link copied')}>Copy Link</button><a className="btn light" href="https://www.messenger.com" target="_blank">Open Messenger</a></div>{copied&&<p className="notice">{copied}</p>}</div>}
